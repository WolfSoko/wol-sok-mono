# Nx 18 enables using plugins to infer targets by default
# This is disabled for existing workspaces to maintain compatibility
# For more info, see: https://nx.dev/concepts/inferred-tasks
NX_ADD_PLUGINS=true
# Force Nx to use ts-node when loading TypeScript configuration files to avoid
# native crashes from @swc-node/register on environments without compatible
# binaries (for example, GitHub Actions' ARM64 runners).
NX_PREFER_TS_NODE=true
# Ensure custom executors that rely on NX_WORKSPACE_ROOT (like the CDK deploy
# plugin) have a sane default when Nx does not inject the variable (for
# example on GitHub-hosted runners).
NX_WORKSPACE_ROOT=.
