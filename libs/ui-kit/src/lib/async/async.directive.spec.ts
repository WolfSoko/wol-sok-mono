import { AsyncDirective } from './async.directive';

describe('AsyncDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncDirective();
    expect(directive).toBeTruthy();
  });
});
