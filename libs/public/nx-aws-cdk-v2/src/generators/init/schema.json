{
  "$schema": "http://json-schema.org/schema",
  "$id": "AwsCdkInit",
  "cli": "nx",
  "title": "Add the AWS CDK dependencies to the workspace",
  "description": "Adds aws-cdk-lib, constructs, the CDK Toolkit CLI and the TypeScript loaders the executors need. Runs automatically as part of the application generator.",
  "type": "object",
  "properties": {
    "unitTestRunner": {
      "description": "The unit test runner to initialise for the workspace.",
      "type": "string",
      "enum": ["jest", "none"],
      "default": "jest"
    },
    "skipFormat": {
      "description": "Skip formatting the touched files with Prettier.",
      "type": "boolean",
      "default": false
    }
  },
  "required": []
}
